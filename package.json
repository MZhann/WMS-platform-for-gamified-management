{
  "name": "wms-platform",
  "version": "1.0.0",
  "private": true,
  "description": "WMS Platform - Backend deploys from apps/backend",
  "scripts": {
    "build": "cd apps/backend && npm install && npm run build",
    "start": "cd apps/backend && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
