# Explicit Nixpacks config for Railway/Railpack
# Ensures Node.js is detected when building from apps/backend

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
